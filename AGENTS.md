# 배포 버전 규칙

- 변경사항을 배포할 때마다 버전을 `V0.1.0 → V0.1.1 → V0.1.2`처럼 마지막 숫자를 1씩 올린다. 사용자 지시 없이 중간 버전을 건너뛰거나 minor/major를 올리지 않는다.
- `Info.plist`의 표시 버전과 빌드 번호를 함께 올린다. 같은 작업의 수정·검증 중에는 버전을 반복해서 올리지 않는다.
- 앱, DMG 이름, 릴리즈 설명의 표시 버전을 일치시킨다. Git 태그는 기존 저장소와 동일하게 소문자 `v` 접두사를 사용한다.
- 자동 업데이트 배포는 `scripts/publish-release.sh`를 이용해 서명된 `appcast.xml`과 DMG를 함께 게시한다. 비공개 업데이트 서명 키를 저장소나 릴리즈에 포함하지 않는다.
